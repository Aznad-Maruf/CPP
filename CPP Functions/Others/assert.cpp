/*
assert( 0 ) -> gives an error.
assert( 1 ) -> nothing happens.
*/

#include<bits/stdc++.h>
using namespace std;

int main(){
    int a_i, b_i, n, temp;
    cin>>n>>temp;
    assert( temp );
    cout<<n/temp<<endl;

    main();
}
